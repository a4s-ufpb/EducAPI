spring:
    datasource:
        password: ${POSTGRES_PASSWORD}
        url: ${DB_URL}
        username: ${POSTGRES_USER}
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: false
        show-sql: false