app:
    token:
        key: ${TOKEN_KEY:token}
    version: ${EDUCAPI_VERSION:1.0.7}
spring:
    profiles:
        active: ${PROFILE_ACTIVE:test}
minio:
    url: http://minio:9000
    bucket: educapi-context-images