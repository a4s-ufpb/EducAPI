app:
    token:
        key: educapi-dev
    version: 1.0.6
spring:
    datasource:
        driverClassName: org.h2.Driver
        password: ''
        url: jdbc:h2:mem:testdb
        username: sa
    jpa:
        hibernate:
            ddl-auto: create-drop
        properties:
            hibernate:
                format_sql: true
        show-sql: true
    minio:
        url: http://minio:9000
        access-key: admin
        secret-key: password123
        bucket: educapi