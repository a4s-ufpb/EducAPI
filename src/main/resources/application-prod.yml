spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: ${DB_PASSWORD}
        url: ${DB_URL}
        username: ${DB_USER}
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: false
        show-sql: false